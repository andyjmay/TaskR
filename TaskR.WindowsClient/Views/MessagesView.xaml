<UserControl
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk" 
    x:Class="TaskR.WindowsClient.Views.MessagesView"
    mc:Ignorable="d"
    d:DesignHeight="300" d:DesignWidth="400">
  <UserControl.DataContext>
    <Binding Mode="OneWay" Path="Messages" Source="{StaticResource Locator}" />
  </UserControl.DataContext>
  <Grid x:Name="LayoutRoot" Background="White">
    <!--<sdk:DataGrid AutoGenerateColumns="False" IsReadOnly="True" ItemsSource="{Binding ReceivedMessages}">
      <sdk:DataGrid.Columns>
        <sdk:DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="150" />
        <sdk:DataGridTextColumn Header="Message" Binding="{Binding Data}" />
      </sdk:DataGrid.Columns>
    </sdk:DataGrid>-->    
  </Grid>
</UserControl>
